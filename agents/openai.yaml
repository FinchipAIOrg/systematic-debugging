interface:
  display_name: "Systematic Debugging"
  short_description: "Find root causes before attempting fixes"
  default_prompt: "Use $systematic-debugging to investigate this failure and identify the root cause before proposing a fix."
